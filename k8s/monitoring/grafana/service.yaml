apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: agrosolutions
  labels:
    app: grafana
    tier: monitoring
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30300
    protocol: TCP
    name: http
