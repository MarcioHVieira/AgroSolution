apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: agrosolutions
  labels:
    app: prometheus
    tier: monitoring
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30090
    protocol: TCP
    name: http
