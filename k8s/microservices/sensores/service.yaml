apiVersion: v1
kind: Service
metadata:
  name: sensores-service
  namespace: agrosolutions
  labels:
    app: sensores
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: sensores
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
