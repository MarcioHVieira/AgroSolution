apiVersion: v1
kind: Service
metadata:
  name: analise-service
  namespace: agrosolutions
  labels:
    app: analise
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: analise
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
