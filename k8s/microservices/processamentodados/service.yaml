apiVersion: v1
kind: Service
metadata:
  name: processamentodados-service
  namespace: agrosolutions
  labels:
    app: processamentodados
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: processamentodados
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
