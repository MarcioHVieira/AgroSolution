apiVersion: v1
kind: Service
metadata:
  name: notificacoes-service
  namespace: agrosolutions
  labels:
    app: notificacoes
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: notificacoes
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
