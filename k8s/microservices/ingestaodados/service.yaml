apiVersion: v1
kind: Service
metadata:
  name: ingestaodados-service
  namespace: agrosolutions
  labels:
    app: ingestaodados
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: ingestaodados
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
